name: getit_issue
description: GetIt reproduce
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter
  lib_common:
    path: lib_common/
  injectable: 1.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: 2.1.2
  pedantic: 1.11.0
  injectable_generator: 1.5.2